package example.datamodel.widthoptional;

import example.datamodel.school.PeoplePageModel;
import example.datamodel.school.SchoolRenderer;
import example.datamodel.school.Student;
import example.datamodel.school.Teacher;
import templ.apt.RendererProxy;

public class OptionalWidthExample {

    public static void main(String[] args) {
        /*String result = RendererProxy.mock(FooBarRenderer.class).renderFoo(new Foo());
        System.out.println(result);

        result = RendererProxy.mock(FooBarRenderer.class).renderFooBar(new FooBar());
        System.out.println(result);

        result = RendererProxy.mock(FooBarRenderer.class).renderFooBar2(new FooBar2());
        System.out.println(result);*/
    }
}